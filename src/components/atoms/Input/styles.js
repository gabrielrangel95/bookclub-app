import styled from 'styled-components/native'
import { fontProps, sizeProps, colorProps } from '~/styles/styled-system'

export const TextInput = styled.TextInput`
  ${fontProps}
  ${sizeProps}
  ${colorProps}
`
